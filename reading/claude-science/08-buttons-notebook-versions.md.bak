# 08 · 界面·功能篇：一个实验看懂交互与版本

*Claude Science 绿皮书 · 第 8 篇 · 2026-07-24*

![Claude Science 绿皮书 08 · 界面·功能篇](/claude-science/day08/cs08-cover.png)

> 第 7 篇用乳腺癌 SVM 案例讲清了科研流程。这一篇我们不换案例，**沿同一条实验流，看 Claude Science 的界面按钮怎么把科研管理起来**。

---

## 一、先看见一个正在跑的实验

讲按钮之前，先看一个任务跑起来之后的界面。

![Claude Science 运行中的主界面，左侧项目栏、中间对话/代码区、右侧产物区](/claude-science/day08/cs08-workspace-labeled.png)

*图 1：任务跑起来后的主界面。三大功能区用色块区分：① 左侧项目/会话栏（蓝）、② 中间对话/Notebook 区（绿）、③ 右侧产物区（橙），顶部同时标出产物区的版本号、收藏、⋮ 菜单与点开大图入口*

这张图不是某个菜单展开后的特写，而是**一个实验正在进行时的默认界面**：

- 左侧是项目与会话导航栏；
- 中间是对话与 Notebook 内容；
- 右侧是这次实验产生的图表、文件和版本记录。

右栏是最值得注意的地方。它把“这次实验有了什么产出”直接摆在你面前，而不是散落在文件夹里。每个产物旁边还有几个操作入口：**版本号**、**收藏星**、**更多菜单**、**点击查看大图**。

Claude Science 本质上是**基于 Jupyter Notebook 做的产品化封装**。它保留了“代码一段一段跑、结果当场落页”的体验，但把项目、会话、环境、产物、版本、审查这些科研管理需求，全部做成了可点击的界面。对一个课题组来说，它不只是聊天工具，而是一个**进度可视、版本可控、可重复性可追溯**的科研项目工作台。

这一篇我们要做的，就是把这个界面上的按钮一个一个讲清楚。

---

## 二、右栏产物区的按钮逐个讲

右栏每个产物都不是静态图片，而是一整条可操作的记录。

![产物 ⋮ 菜单：9 项生命周期管理](/claude-science/day08/cs08-artifact-menu-labeled.png)

*图 2：产物区右上角的 ⋮ 菜单展开后，共有 9 项功能。我把每项按顺序标号，方便你对照*

**⋮ 菜单里的每一项都在回答一个具体问题：**

- **1 Star 收藏**：这张图我后面还要反复看，先收藏；
- **2 Hide 隐藏**：暂时不需要它显示在产物区，但不想删；
- **3 View in context 回看上下文**：它是在哪一轮对话、哪段代码里生成的？一键回去；
- **4 Provenance 溯源**：查看它由哪段代码、哪个 prompt 生成；
- **5 Copy link 分享链接**：复制一个链接，发给合作者；
- **6 Rename 重命名**：整理归档；
- **7 Export Metadata 导出元数据**：把产物信息导出到本地；
- **8 Export to Cloud 导出到云**：同步到云端目录；
- **9 Delete 删除**：确认不再需要后清理。

更关键的是版本号。

![feature_importance.png 旁边显示 v2，版本号已放大标红](/claude-science/day08/cs08-versions-labeled.png)

*图 3：`feature_importance.png` 旁边显示 `v2`。每次改动都会自动叠加新版本，想回看旧版直接点版本号切换*

`v2` 不是文件名后缀，而是系统记录的版本号。你每改一次 prompt、每重跑一次代码，产出都会自动叠新版本。想回看之前长什么样，点版本号就能切换。

这就是科研里常说的**可重复性**：不是只保存最终图，而是保存**得到这张图的全过程**。

---

## 三、点开 Messages：任务是怎么被拆出来的

右栏管的是产物，中间和左侧管的是过程。先看最上面的任务理解层。

![Notebook Messages 标签：AI 读取任务书并拆解步骤](/claude-science/day08/cs08-messages.png)

*图 4：Messages 标签里，AI 正在读取任务书并拆解步骤*

这张图里，我给它下了一个明确指令：

> 用 SVM 算法对乳腺癌进行良性/恶性肿瘤分类。

紧接着它把任务拆成 5 步：数据加载与 EDA、SVM 训练与评估、模型解释与可视化、输出研究报告。每一步都有清晰的输入输出。

**Messages 标签不是普通聊天记录，而是 AI 的执行日志：**

- 它理解了什么；
- 它准备做什么；
- 它已经完成了哪一步。

你可以把它当成实验的**任务看板**。想确认 AI 有没有跑偏，先看 Messages。

---

## 四、点开 Environment：先看环境再看代码

任务理解后，AI 不会直接甩一段代码给你。它会先建一个独立环境，并把依赖包列出来。

![Notebook Environment 标签：当前环境包列表](/claude-science/day08/cs08-environment.png)

*图 5：Environment 标签显示当前实验的独立环境*

这张图里有三个关键信息：

- **Environment: ml-bc** —— 专属于这个实验的 Conda 环境；
- **Python 3.13.14** —— 明确的 Python 版本；
- **Packages 152** —— 当前环境里装了哪些包、什么版本。

科研里最忌讳的就是“在我电脑上能跑”。**Claude Science 把环境也做成实验记录的一部分**，别人复现时照着这个包列表就能还原。

---

## 五、点开 Code：Notebook 细胞式执行

环境 ready 后，才进入真正的代码。

![Notebook Code 标签：figure-style-kernel.py 被注入并执行](/claude-science/day08/cs08-code.png)

*图 6：Code 标签里，skill 自动注入的样式代码正在执行*

这张图里最值得注意的不是代码本身，而是顶部的标签栏：

> **Code · Execution Log · Messages · Environment · Review**

它把一个实验拆成了五个视图：

- **Code**：当前运行的代码细胞；
- **Execution Log**：每次运行的输出；
- **Messages**：任务理解与执行计划；
- **Environment**：依赖环境；
- **Review**：审查结果。

这种设计和 Jupyter Notebook 很像，但多了两层：**任务理解层（Messages）和审查层（Review）**。这正是它比纯 Notebook 更适合做科研的地方。

另外注意代码第一行的注释：

```python
# skill:figure-style-kernel.py (auto-injected on skill load)
```

说明这段代码不是手写进去的，而是**skill 自动注入**的。Claude Science 会把重复使用的样式、工具封装成 skill，每次新建相关实验时自动加载。

---

## 六、点开 annotate：图上圈哪里问哪里

代码跑完，图就出来了。但这里的重点不是“看图”，而是**“问图”**。

![在 eda_distribution.png 的 Panel B 上标注“这个图有什么含义”，AI 定位并解释](/claude-science/day08/cs08-annotate.png)

*图 7：在图上圈出 Panel B 并提问，AI 专门解释这块区域*

你看到我在图上圈了一块区域，并写了一句话：

> 这个图有什么含义？

AI 没有泛泛而谈，而是先读我的标注位置，然后专门针对 **Panel B — Mean Radius by Diagnosis** 解释：

- 横轴是 Mean Radius；
- 红色是恶性肿瘤，蓝色是良性；
- 恶性分布整体右移，说明恶性肿瘤的细胞核更大。

这个功能的价值在于：**你不用再费力用文字描述“我看的是图的哪一部分”**，直接圈出来，AI 自己定位、自己回答。

对科研来说，这意味着你可以对着复杂图表快速追问：

- “这个峰值代表什么？”
- “这两个分布重叠说明什么？”
- “这个离群点要不要剔除？”

---

## 七、点开 + 菜单：加料入口

前面讲的都是实验跑起来之后的事。但实验开始前，你总得往对话里加点东西吧？

![输入框左下角的 + 菜单：Attach files / Your files / Request review / Save as skill / Context usage](/claude-science/day08/cs08-plus-menu.png)

*图 8：输入框左下角的 + 菜单，把五种常用操作收在一个入口*

这就是第 5 篇漏讲、第 8 篇要补上的那个按钮。它把五种常用操作收在一个入口：

- **Attach files**：上传数据、文献、图片；
- **Your files**：查看项目里已有的文件；
- **Request review**：手动触发审查员复查；
- **Save as skill**：把当前这段对话/代码存成可复用 skill；
- **Context usage**：查看当前对话用了多少 token。

把它放在最后讲，是因为它和其它六步形成一个完整闭环：

> **+ 号加料 → Messages 拆任务 → Environment 建环境 → Code 跑细胞 → 图上圈问 → 产物管理版本**

不过要注意：**+ 号是补充入口，不是主流程的第一步**。真正驱动实验往前走的是 
**Messages → Environment → Code → annotate → 产物区** 这条主线。+ 号只是在需要时往对话里加文件、加审查或加 skill。

这也是图 9 总结图里那条主线的全部内容。

---

## 八、一张图带走实验流

如果你嫌按钮多记不住，最后这张图把整篇文章串成一条主线。

注意：**+ 号是补充入口**（加文件 / 加审查 / 加 skill），真正串起实验的是下面这条主线：

![Claude Science 界面功能篇总结：一张图看懂实验流](/claude-science/day08/cs08-summary.png)

*图 9：实验主流程 Messages → Environment → Code → annotate → 产物区版本管理；+ 号作为补充入口*

这张图也是本篇的导航图：下次不知道要点哪个按钮时，先问自己——我现在在实验的哪个阶段？然后回到这张图里找对应入口即可。

---

## 九、写在最后：按钮是实验流的入口

学完这一篇，你应该对 Claude Science 的界面有另一种理解：

- 它不是聊天框的装饰品；
- 也不是 Jupyter 的简单复刻；
- 它是把**科研流程固化成交互按钮**的工作台。

一张表带走：

| 按钮/标签 | 在实验流里的位置 | 什么时候用 |
|---|---|---|
| Messages | 任务理解阶段 | 确认 AI 拆对了步骤 |
| Environment | 准备阶段 | 检查依赖环境是否可复现 |
| Code | 执行阶段 | 一段一段跑代码 |
| 图上圈注 | 解释阶段 | 对着图表提问 |
| 产物 ⋮ 菜单 / v2 | 产物区 | 管理图表生命周期、回溯版本 |
| + 号 | 实验开始前/补充入口 | 加文件、加审查、加 skill |

下一篇，我们不讲新功能，而是讲一件更实际的事：**数据导出与备份**。毕竟云端账号可能遇到各种状况，只有把实验产物、代码、环境记录定期导出到本地或私有仓库，才是真正的科研安全感。

---

## 一句带走

Claude Science 的按钮，本质上是把科研的环节——**理解、环境、执行、解释、归档、迭代**——变成了一块块可点击的积木；而 + 号，就是往积木盒里随时补料的入口。
